<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 120">
  <defs>
    <linearGradient id="turosGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#00eaff"/>
      <stop offset="100%" stop-color="#0066ff"/>
    </linearGradient>
  </defs>

  <!-- Background circle -->
  <circle cx="60" cy="60" r="56" fill="#030712" stroke="url(#turosGrad)" stroke-width="4"/>

  <!-- Inner glow ring -->
  <circle cx="60" cy="60" r="48" fill="none" stroke="url(#turosGrad)" stroke-width="1" opacity="0.35"/>

  <!-- Code symbol -->
  <text x="60" y="78" font-family="Segoe UI, Arial, sans-serif" font-size="46" font-weight="900"
        text-anchor="middle" fill="url(#turosGrad)">&lt;/&gt;</text>
</svg>
